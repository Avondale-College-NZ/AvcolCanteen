@model AvcolCanteen.Models.Orders

@{
    ViewData["Title"] = "Create";
    var currentDate = DateTime.Now;
    var minDate = currentDate.ToString("yyyy-MM-dd");
    var maxDate = currentDate.AddDays(7).ToString("yyyy-MM-dd");
}

<h1>Create</h1>

<h4>Orders</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="AvcolCanteenUserID" class="control-label"></label>
                <select asp-for="AvcolCanteenUserID" class ="form-control" asp-items="ViewBag.AvcolCanteenUserID"></select>
            </div>
            <div class="form-group">
                <label asp-for="Date" class="control-label"></label>
                <input asp-for="Date" class="form-control" type="date" min="@minDate" max="@maxDate" />
                <span asp-validation-for="Date" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
